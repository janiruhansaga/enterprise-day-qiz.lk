{
  "name": "mindpulse-arena",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm ci --include=dev --prefix client && npm ci --prefix server",
    "build": "npm run build --prefix client",
    "vercel-build": "npm ci --include=dev --prefix client && npm run build --prefix client",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server"
  }
}